Name:   Yehonathan Jacob
Id:     316304740
Date:   05/03/2020
*****

DecisionTreeImpl(DataSet train):
    The function call recursion to build the tree.

treeForChild:
    Building a node. If it is a leaf, so just node with no childrens, otherwise, calculate a build also  node childs.

priorityLable:
    return the most priority label to get picked by the current child.

entropy, Remainder_A:
    implement the algorithem from Pg 704.

classify:
    try to get to the best result, due to a build tree.

rootInfoGain:
    print the hole tree

printAccuracy:
    calc the accuracy acordding to result of test on traind tree.
